---
phase: 01-data-foundation-persistence
plan: 04
type: execute
wave: 3
depends_on: [01-03]
files_modified:
  - src/components/SetGrid.tsx
autonomous: true
requirements: [PERS-03]
gap_closure: true
must_haves:
  truths:
    - "Each set card in SetGrid displays owned vs total counts plus a percentage derived from persisted ownedCards."
    - "Completion indicators react immediately to ownership toggles because they read directly from useCollection state."
    - "After a browser reload, completion badges still reflect true ownership since stats recompute from localStorage."
  artifacts:
    - path: "src/components/SetGrid.tsx"
      provides: "Owned count, percentage badge, and progress bar for every set card"
      contains: ["useCollection", "useMemo", "<Progress value"]
  key_links:
    - from: "src/components/SetGrid.tsx"
      to: "src/lib/collection.ts"
      via: "useCollection() destructuring ownedCards"
      pattern: "const \\{ ownedCards"
    - from: "src/components/SetGrid.tsx"
      to: "src/components/ui/progress.tsx"
      via: "Progress component renders completion bar"
      pattern: "<Progress"
---

<objective>
Expose true set completion progress inside SetGrid using the existing ownedCards persistence so requirement PERS-03 is satisfied.

Purpose: The data layer already tracks owned cards, but no UI shows set-level completion. Surfacing percentages ensures users can verify progress and that statistics persist through reloads.
Output: Updated SetGrid component with computed stats, progress bar, and completion badge per set card.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-data-foundation-persistence/01-VERIFICATION.md
@src/components/SetGrid.tsx
@src/lib/collection.ts
@src/components/ui/progress.tsx

<interfaces>
From src/lib/collection.ts:
```typescript
export const useCollection: () => {
  ownedCards: Record<string, boolean>;
  isInCollection(cardId: string): boolean;
  addToCollection(cardOrId: string | { id: string }): void;
  removeFromCollection(cardId: string): void;
}
```

From src/components/ui/progress.tsx:
```typescript
export const Progress: React.ForwardRefExoticComponent<
  React.ComponentPropsWithoutRef<typeof ProgressPrimitive.Root>
>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire owned-card state into SetGrid for completion math</name>
  <files>src/components/SetGrid.tsx</files>
  <read_first>
    - src/components/SetGrid.tsx (current grid rendering)
    - src/lib/collection.ts (useCollection ownedCards contract)
  </read_first>
  <action>
  Extend SetGrid logic so completion stats are available for rendering:

  1. Import `useMemo` from React if not already and `useCollection` from `@/lib/collection`.
  2. Destructure `ownedCards` from `useCollection()` near other hooks; no other collection actions are needed.
  3. Add a `useMemo` block that builds a `Record<string, { owned: number; percentage: number }>` keyed by set ID. Count owned cards by splitting each stored card ID on `-` (TCGdex IDs follow `{setId}-{cardNumber}`) and tallying matches.
  4. Within the memo, guard against divide-by-zero by returning zeroes when `set.total` is falsy and clamp percentages to 0-100 with `Math.min(Math.max(value, 0), 100)`.
  5. Expose a helper (e.g., `const getCompletion = (setId: string, total: number) => ...`) that returns `{ owned, percentage, total }` so the rendering layer reads from a single source of truth.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/SetGrid.tsx</automated>
  </verify>
  <done>SetGrid computes memoized owned counts and percentages per set directly from useCollection. No UI yet.</done>
</task>

<task type="auto">
  <name>Task 2: Render per-set completion indicators in SetGrid</name>
  <files>src/components/SetGrid.tsx</files>
  <read_first>
    - src/components/SetGrid.tsx (after Task 1 logic is in place)
    - src/components/ui/progress.tsx (Progress component usage)
  </read_first>
  <action>
  Surface the computed stats in the UI so users see completion at a glance:

  1. Import the `Progress` component from `@/components/ui/progress`.
  2. Inside each set card (e.g., within `CardContent` under the total card count), render:
     - A text row such as `"Owned X / Y"` with `completion.percentage.toFixed(0)` appended.
     - A `<Progress value={completion.percentage}` element with `aria-label` describing the owned count for accessibility.
  3. If `completion.percentage === 100`, show a `Badge` (variant `outline` or success) labeled `"Complete"` separate from legality badges so 100% sets stand out.
  4. Ensure the layout remains responsive (stacked on small screens, flex on larger). Keep spacing consistent with existing Tailwind classes and reuse muted foreground colors for secondary text.
  5. Update any conditional rendering to rely on the helper from Task 1 so completion UI updates immediately when ownedCards changes and still works after reload.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/SetGrid.tsx</automated>
  </verify>
  <done>Each set card shows owned vs total text, a progress bar, and a completion badge at 100%, all driven by persisted ownedCards.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit src/components/SetGrid.tsx` — confirms new logic compiles.
- Manual (post-execution): Toggle card ownership, return to Sets view, and observe progress updates instantly and after a browser reload.
</verification>

<success_criteria>
- SetGrid cards show accurate owned/total counts plus progress bars sourced from persisted ownedCards.
- Completion badges appear only for 100% owned sets and remain after reload.
- Requirement PERS-03 is satisfied because stats are now user-visible and tied to persisted state.
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation-persistence/01-04-SUMMARY.md`
</output>
