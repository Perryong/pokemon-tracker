---
phase: 06-testing-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.config.ts
  - src/test/setup.ts
  - src/components/__tests__/CardGrid.test.tsx
  - src/lib/__tests__/collection.test.ts
  - src/lib/__tests__/migration.test.ts
autonomous: true
requirements: [TESTQ-01, TESTQ-02]

must_haves:
  truths:
    - "Baseline test suite no longer fails from matcher/runtime setup issues."
    - "Quantity control tests cover increment, decrement, fast toggle, zero-floor, and 0/999 boundaries."
    - "Quantity updates are verified to persist to localStorage with sparse zero-key removal."
    - "Migration large-payload automation (thousands of cards) passes deterministically."
  artifacts:
    - path: "src/test/setup.ts"
      provides: "Shared Vitest/RTL matcher bootstrap via jest-dom."
      contains: ["@testing-library/jest-dom/vitest"]
    - path: "src/components/__tests__/CardGrid.test.tsx"
      provides: "Comprehensive TESTQ-02 component coverage for quantity controls."
      contains: ["Increase quantity", "Decrease quantity", "fast toggle", "zero-floor"]
    - path: "src/lib/__tests__/collection.test.ts"
      provides: "Hook-level boundary and persistence assertions for quantity APIs."
      contains: ["setQuantity", "incrementQuantity", "decrementQuantity", "localStorage"]
    - path: "src/lib/__tests__/migration.test.ts"
      provides: "Large-payload migration regression assertions."
      contains: ["5000", "10000"]
  key_links:
    - from: "vitest.config.ts"
      to: "src/test/setup.ts"
      via: "test.setupFiles"
      pattern: "setupFiles"
    - from: "src/components/__tests__/CardGrid.test.tsx"
      to: "src/components/CardGrid.tsx"
      via: "accessible labels and toggle contract assertions"
      pattern: "Increase quantity|Decrease quantity|Add|Remove"
    - from: "src/lib/__tests__/collection.test.ts"
      to: "src/lib/collection.ts"
      via: "quantity API + persistence contract"
      pattern: "setQuantity|incrementQuantity|decrementQuantity|STORAGE_KEY"
---

<objective>
Stabilize the test baseline first, then complete comprehensive automated coverage for quantity controls without expanding product scope.

Purpose: Unblock Phase 6 by fixing root-cause test harness failures and hardening TESTQ-02 with low-risk, behavior-accurate assertions.
Output: Green baseline for quantity suites, complete TESTQ-02 coverage, and deterministic large-payload migration evidence.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-testing-validation/06-CONTEXT.md
@.planning/phases/06-testing-validation/06-RESEARCH.md
@.planning/phases/05-quantity-ui-statistics/05-CONTEXT.md
@src/components/__tests__/CardGrid.test.tsx
@src/lib/__tests__/collection.test.ts
@src/lib/__tests__/migration.test.ts
@vitest.config.ts

<interfaces>
From src/lib/collection.ts (already implemented contract):
```ts
getQuantity(cardId: string): number;
setQuantity(cardId: string, quantity: number): void;      // clamp 0..999
incrementQuantity(cardId: string): void;                  // capped at 999
decrementQuantity(cardId: string): void;                  // floored at 0
addToCollection(cardOrId: string | { id: string }): void; // fast toggle/add path
removeFromCollection(cardId: string): void;               // fast remove path
cardQuantities: Record<string, number>;                   // sparse qty>0 only
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repair baseline test harness and matcher setup</name>
  <files>vitest.config.ts, src/test/setup.ts, src/components/__tests__/CardGrid.test.tsx</files>
  <action>Add `src/test/setup.ts` importing `@testing-library/jest-dom/vitest`, wire it in `vitest.config.ts` via `test.setupFiles`, and move any nested/hoist-unsafe `vi.mock(...)` usage in `CardGrid.test.tsx` to top-level declarations. Keep behavior assertions intact; do NOT weaken assertions to bypass failures. This addresses known baseline blocker (`Invalid Chai property: toBeInTheDocument`) per Phase 6 locked decisions.</action>
  <verify>
    <automated>npm run test -- src/components/__tests__/CardGrid.test.tsx --run</automated>
  </verify>
  <done>CardGrid suite runs without matcher-runtime errors and without mock-hoist warnings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete TESTQ-02 quantity-control coverage with boundary + persistence assertions</name>
  <files>src/components/__tests__/CardGrid.test.tsx, src/lib/__tests__/collection.test.ts</files>
  <behavior>
    - Test: increment path triggers quantity increase behavior and remains capped at 999.
    - Test: decrement path never goes below 0 and minus control reflects zero-floor.
    - Test: fast toggle behavior preserves `0 ↔ 1` and clears from `&gt;1 → 0` ownership semantics.
    - Test: quantity updates persist to localStorage after set/increment/decrement operations.
    - Test: sparse storage contract holds (quantity 0 removes card key).
  </behavior>
  <action>Expand existing suites using stable, semantics-first selectors (scoped by card identity rather than index assumptions). Cover all locked TESTQ-02 behaviors: increment, decrement, fast toggle, zero-floor, boundary 0/999, and persistence. Prefer test updates over production edits; only touch implementation if a genuine contract mismatch is discovered and document why in summary.</action>
  <verify>
    <automated>npm run test -- src/components/__tests__/CardGrid.test.tsx --run && npm run test -- src/lib/__tests__/collection.test.ts --run</automated>
  </verify>
  <done>TESTQ-02 is fully automated with deterministic assertions and both suites pass.</done>
</task>

<task type="auto">
  <name>Task 3: Reinforce TESTQ-01 evidence with deterministic large-payload migration regression</name>
  <files>src/lib/__tests__/migration.test.ts</files>
  <action>Add/adjust one deterministic large-payload migration test (thousands of cards, patterned ownership) to complement existing 5000-card coverage and prevent random/flaky behavior. Keep runtime lean (single high-value scenario), preserve current migration behavior contracts, and avoid architecture changes.</action>
  <verify>
    <automated>npm run test -- src/lib/__tests__/migration.test.ts --run</automated>
  </verify>
  <done>Migration automation includes deterministic large-payload validation and remains green.</done>
</task>

</tasks>

<risk_notes>
- Primary risk: brittle selectors in CardGrid tests causing false regressions after harmless UI ordering changes.
- Mitigation: use role/label + scoped queries; avoid index-based assertions.
- Secondary risk: over-editing production code while fixing tests.
- Mitigation: keep edits test-first and minimal; only patch implementation for proven contract violations.
</risk_notes>

<rollback_or_adjustment>
- If harness changes create broader suite instability, revert to prior `vitest.config.ts` + setup integration as one atomic rollback, then reapply incrementally.
- If new TESTQ-02 assertions expose real behavior mismatches, adjust only affected quantity functions (`setQuantity`, `incrementQuantity`, `decrementQuantity`) and rerun targeted suites before full test run.
</rollback_or_adjustment>

<verification>
Run in order:
1. `npm run test -- src/components/__tests__/CardGrid.test.tsx --run`
2. `npm run test -- src/lib/__tests__/collection.test.ts --run`
3. `npm run test -- src/lib/__tests__/migration.test.ts --run`
4. `npm run test`
Expected outcome: no baseline matcher/runtime failures, all targeted suites green, full test suite pass.
</verification>

<definition_of_done>
- Baseline failure class (`toBeInTheDocument` runtime mismatch) is eliminated.
- TESTQ-02 behaviors are fully covered with automated tests and passing.
- Large-payload migration test evidence remains passing for TESTQ-01 confidence.
- No scope expansion beyond testing/validation hardening.
</definition_of_done>

<success_criteria>
1. `npm run test` passes after baseline stabilization and coverage expansion.
2. Quantity controls have explicit automated coverage for increment/decrement/toggle/zero-floor/0..999/persistence.
3. Migration large-payload automation remains passing and deterministic.
</success_criteria>

<output>
After completion, create `.planning/phases/06-testing-validation/06-01-SUMMARY.md`
</output>
