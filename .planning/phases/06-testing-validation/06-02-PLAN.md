---
phase: 06-testing-validation
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/components/__tests__/SetGrid.regression.test.tsx
  - src/components/__tests__/CrossViewStats.test.tsx
  - src/components/__tests__/App.persistence-regression.test.tsx
  - src/components/__tests__/CardGrid.test.tsx
autonomous: true
requirements: [TESTQ-03]

must_haves:
  truths:
    - "v1.0 set browsing and filter behavior remains correct after quantity changes."
    - "Album ownership interactions still work in regression paths after quantity coverage hardening."
    - "localStorage-backed collection state persists across remount/session simulation."
    - "CardGrid, SetGrid, CollectionView, and CollectionStats stay aligned on unique-vs-total semantics."
    - "Final release gate evidence shows both test and build pass."
  artifacts:
    - path: "src/components/__tests__/SetGrid.regression.test.tsx"
      provides: "Regression coverage for set browsing and filter contracts."
      contains: ["search", "clear filters", "onSetSelect"]
    - path: "src/components/__tests__/App.persistence-regression.test.tsx"
      provides: "Remount/session persistence verification against localStorage."
      contains: ["render", "unmount", "remount", "STORAGE_KEY"]
    - path: "src/components/__tests__/CrossViewStats.test.tsx"
      provides: "Cross-view consistency assertions for unique-owned vs total-quantity semantics."
      contains: ["SetGrid", "CollectionView", "CollectionStats", "Total Qty"]
  key_links:
    - from: "src/components/__tests__/SetGrid.regression.test.tsx"
      to: "src/components/SetGrid.tsx"
      via: "filter UI + set selection callback"
      pattern: "Search sets|Clear Filters|onSetSelect"
    - from: "src/components/__tests__/App.persistence-regression.test.tsx"
      to: "src/lib/collection.ts"
      via: "hydration from STORAGE_KEY across remount"
      pattern: "localStorage|getInitialState"
    - from: "src/components/__tests__/CrossViewStats.test.tsx"
      to: "src/components/{CardGrid,SetGrid,CollectionView,CollectionStats}.tsx"
      via: "shared fixture assertions for unique vs total"
      pattern: "Owned|Total Qty|Unique Cards|Total Quantity"
---

<objective>
Deliver TESTQ-03 regression hardening across core v1.0 workflows and finish the strict Phase 6 evidence gate.

Purpose: Prove quantity changes did not regress core browse/filter/persistence/interaction behavior and that cross-view stats semantics remain coherent.
Output: New regression suites plus final gate evidence (`npm run test && npm run build`) for phase completion.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-testing-validation/06-CONTEXT.md
@.planning/phases/06-testing-validation/06-RESEARCH.md
@.planning/phases/05-quantity-ui-statistics/05-CONTEXT.md
@src/components/SetGrid.tsx
@src/components/CollectionView.tsx
@src/components/CollectionStats.tsx
@src/components/__tests__/CardGrid.test.tsx

<interfaces>
From src/components/SetGrid.tsx:
```ts
interface SetGridProps {
  onSetSelect: (set: PokemonSet) => void;
}
```

Key semantic outputs:
```tsx
Owned {completion.owned} / {completion.total}
{completion.totalQuantity > completion.owned && <div>Total Qty: {completion.totalQuantity}</div>}
```

From src/components/CollectionView.tsx / CollectionStats.tsx:
```tsx
Unique Cards
Total Quantity
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TESTQ-03 regression suites for set browsing, filters, and persistence remount behavior</name>
  <files>src/components/__tests__/SetGrid.regression.test.tsx, src/components/__tests__/App.persistence-regression.test.tsx</files>
  <behavior>
    - Test: set cards render from mocked set data and selecting a set invokes `onSetSelect` with expected set.
    - Test: search/filter interactions narrow visible sets and clear-filters restores baseline set list.
    - Test: collection state persists across unmount/remount using localStorage-backed hydration.
  </behavior>
  <action>Create focused regression suites that validate existing v1.0 workflows after quantity introduction. Reuse existing mock patterns from current test files; keep tests deterministic and fast. Cover only scope-locked TESTQ-03 behaviors (set browsing, filters, persistence). Avoid adding new UI features or refactoring production components unless tests prove a real regression.</action>
  <verify>
    <automated>npm run test -- src/components/__tests__/SetGrid.regression.test.tsx --run && npm run test -- src/components/__tests__/App.persistence-regression.test.tsx --run</automated>
  </verify>
  <done>Regression suites for browsing/filter/persistence are added and passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add cross-view consistency regression assertions and keep album interaction regression green</name>
  <files>src/components/__tests__/CrossViewStats.test.tsx, src/components/__tests__/CardGrid.test.tsx</files>
  <behavior>
    - Test: given shared quantity fixture, SetGrid/CollectionView/CollectionStats report consistent unique-owned and total-quantity semantics.
    - Test: album ownership interactions remain valid in regression suite after quantity updates (add/remove/toggle path still functioning).
  </behavior>
  <action>Create a lightweight cross-view test using a single controlled fixture to assert semantic agreement across all required views (`CardGrid`, `SetGrid`, `CollectionView`, `CollectionStats`). If needed, extend existing CardGrid regression tests to preserve v1.0 interaction assurance. Keep assertions contract-level and readable for checker audit.</action>
  <verify>
    <automated>npm run test -- src/components/__tests__/CrossViewStats.test.tsx --run && npm run test -- src/components/__tests__/CardGrid.test.tsx --run</automated>
  </verify>
  <done>Cross-view consistency is automated and album interaction regressions remain passing.</done>
</task>

<task type="auto">
  <name>Task 3: Execute strict final evidence gate and capture phase-ready proof</name>
  <files>.planning/phases/06-testing-validation/06-02-SUMMARY.md</files>
  <action>Run full suite + production build gate exactly as locked in Phase 6 context: `npm run test && npm run build`. Record outcomes in summary with pass/fail counts and explicit note that baseline failures were resolved. If failures appear, fix root causes (tests or implementation) and rerun gate until green; do not relax assertions.</action>
  <verify>
    <automated>npm run test && npm run build</automated>
  </verify>
  <done>Final gate passes with documented evidence suitable for Phase 6 completion.</done>
</task>

</tasks>

<risk_notes>
- Risk: SetGrid regression tests can become fragile due to UI-library wrappers (Select/Popover).
- Mitigation: favor user-visible text/role assertions and mock hooks (`useSets`, `useSeries`, `useCollection`) with stable fixtures.
- Risk: cross-view suite could become too integrated/flaky.
- Mitigation: one fixture-driven suite only; keep assertions semantic and minimal.
</risk_notes>

<rollback_or_adjustment>
- If new regression file adds instability, isolate failing scenario, revert only that test block, and reintroduce with narrower scope.
- If strict gate fails on non-Phase-6 unrelated tests, document failure source, patch minimal root cause, and rerun full gate before sign-off.
</rollback_or_adjustment>

<verification>
Run in order:
1. `npm run test -- src/components/__tests__/SetGrid.regression.test.tsx --run`
2. `npm run test -- src/components/__tests__/App.persistence-regression.test.tsx --run`
3. `npm run test -- src/components/__tests__/CrossViewStats.test.tsx --run`
4. `npm run test`
5. `npm run build`
Expected outcome: TESTQ-03 regressions covered and green; final phase gate (`test + build`) passes.
</verification>

<definition_of_done>
- TESTQ-03 regression automation exists for set browsing, filters, persistence, and album interaction continuity.
- Cross-view consistency checks verify unique-vs-total semantics across CardGrid, SetGrid, CollectionView, and CollectionStats.
- Final strict gate evidence (`npm run test && npm run build`) is passing and documented.
- No scope expansion beyond testing/validation hardening.
</definition_of_done>

<success_criteria>
1. All newly added regression suites pass consistently.
2. Full `npm run test` and `npm run build` both pass.
3. Evidence is captured in plan summary for checker verification.
</success_criteria>

<output>
After completion, create `.planning/phases/06-testing-validation/06-02-SUMMARY.md`
</output>
