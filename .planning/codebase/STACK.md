# Technology Stack

**Analysis Date:** 2026-03-20

## Languages

**Primary:**
- TypeScript - Frontend application and UI components in `src/**/*.ts` and `src/**/*.tsx` (entrypoints in `src/main.tsx`, `src/App.tsx`)

**Secondary:**
- CSS - Styling in `src/index.css` and `src/App.css`
- SQL - Database schema migrations in `supabase/migrations/20250518150840_sunny_scene.sql` and `supabase/migrations/20250518150930_empty_silence.sql`

## Runtime

**Environment:**
- Node.js runtime for tooling/scripts (configured via `package.json` scripts and Vite config in `vite.config.ts`)
- Browser runtime for app execution (mounted in `src/main.tsx`, HTML entry in `index.html`)

**Package Manager:**
- npm (scripts and dependency manifest in `package.json`)
- Lockfile: present (`package-lock.json`)

## Frameworks

**Core:**
- React 18 (`react`, `react-dom`) - UI rendering and state management in `src/App.tsx`, `src/main.tsx`
- Vite 5 (`vite`, `@vitejs/plugin-react`) - Dev server and build pipeline in `vite.config.ts` and `package.json` scripts
- Tailwind CSS 3 (`tailwindcss`, `tailwindcss-animate`) - Utility-first styling in `tailwind.config.js`, `postcss.config.js`, and `src/index.css`
- Radix UI + shadcn/ui component layer (`@radix-ui/*`) - Reusable UI primitives in `src/components/ui/*.tsx`

**Testing:**
- Not detected (no `jest.config.*`, `vitest.config.*`, or test scripts in `package.json`)

**Build/Dev:**
- TypeScript compiler (`typescript`) - Type checking/build step via `tsc -b` in `package.json`
- ESLint 9 + typescript-eslint - Linting in `eslint.config.js` and `npm run lint`
- PostCSS + Autoprefixer - CSS processing in `postcss.config.js`

## Key Dependencies

**Critical:**
- `react` / `react-dom` - Application framework and rendering (`src/main.tsx`, `src/App.tsx`)
- `next-themes` - Theme provider and theme-aware UI (`src/App.tsx`, `src/components/ui/sonner.tsx`)
- `lucide-react` - Icon set used in UI (`src/components/Navbar.tsx`)
- `zod` and `react-hook-form` ecosystem - Form/type validation utilities used by shared UI form primitives (`src/components/ui/form.tsx`)

**Infrastructure:**
- `@vitejs/plugin-react`, `vite` - Build/dev infrastructure (`vite.config.ts`, `package.json`)
- `tailwindcss`, `postcss`, `autoprefixer` - Styling pipeline (`tailwind.config.js`, `postcss.config.js`)
- `@radix-ui/*` packages - Accessibility-focused base components (`src/components/ui/*.tsx`)

## Configuration

**Environment:**
- Environment file present: `.env` (exists at project root; treat as secret-bearing config)
- App integration values are currently hardcoded for external API access in `src/lib/api.ts` rather than loaded from `import.meta.env`
- Supabase-related environment variable names are present in `.env` (file exists; values are secret and not documented)

**Build:**
- TypeScript project references in `tsconfig.json` → `tsconfig.app.json` and `tsconfig.node.json`
- Vite alias configuration (`@` -> `./src`) in `vite.config.ts`
- Tailwind scanning/theme config in `tailwind.config.js`
- ESLint flat config in `eslint.config.js`

## Platform Requirements

**Development:**
- Node.js + npm installed to run `npm run dev`, `npm run build`, and `npm run lint` from `package.json`
- Browser-compatible environment for React SPA entrypoint `index.html` -> `src/main.tsx`

**Production:**
- Static frontend hosting target for Vite build output (`dist/` directory present, generated by `vite build`)
- Optional Supabase backend provisioning implied by SQL migrations in `supabase/migrations/*.sql`

---

*Stack analysis: 2026-03-20*
